import React from 'react';

const Bookmark = (props) => {
    return (
        <div>
            <div
            className=''>
                <h1>{props.headline}</h1>
            </div>
        </div>
    );
};

export default Bookmark;